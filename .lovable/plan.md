This is a Lovable Cloud-managed project. The Supabase project itself does not have a separate public name or dashboard you can access directly — it is managed behind the Lovable Cloud backend.

- The Lovable project is **Whoop Companion** (from the published URL `whoop-companion.lovable.app`).
- You can rename the project inside Lovable by clicking the project name in the top-left and choosing **Rename project**.
- If you need the Supabase project reference for a configuration, it is stored only in the project secrets and not exposed in the UI. Ask me if you need help with backend configuration or migrations.

## No code changes needed

This is purely informational. Nothing to implement.